# Test repo for includedScripts + target defaults

Unable to reproduce issue.

- Included scripts specifies only `test`
- Target defaults specifies `build`
- `nx show project --json false` doesn't show `build` as a target